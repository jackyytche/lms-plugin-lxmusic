# t/Slim/Web/Settings.pm — 存根（本地 perl -c 用；设备上用真模块）
package Slim::Web::Settings;

use strict;
use warnings;

sub new  { bless {}, shift }
sub name { '' }
sub page { '' }
sub prefs { }
sub handler { }

1;
